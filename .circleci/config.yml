version: 2
jobs:
  hello_world:
    steps:
      - run: echo "Hello World"
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/repo
    steps:
      - checkout
      - run: echo "I'm Build"
